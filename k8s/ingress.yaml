apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hubii-ingress
spec:
  rules:
    - host: hubii.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hubii-service
                port:
                  number: 80